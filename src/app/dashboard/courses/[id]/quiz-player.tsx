"use client";

import { useRef, useState } from "react";
import { createClient } from "@/lib/supabase/client";

type QuestionType = "multiple_choice" | "multiple_select" | "complete" | "numerical" | "true_false" | "matching" | "ordering" | "image" | "audio" | "reading";
type Question = { id:string; question:string; options:string[]; correctIndex:number; correctAnswer?:string|null; points?:number; media_url?:string|null; media_type?:string|null; reading_passage?:string|null; type:QuestionType };
type Attempt={score:number;total:number};
type Answer=string|number|number[]|Record<string,number>;

function parseNumbers(value:string|null|undefined,fallback:number[]=[]){try{const parsed=JSON.parse(value??"");return Array.isArray(parsed)?parsed.map(Number):fallback;}catch{return fallback;}}
function parseMap(value:string|null|undefined){try{const parsed=JSON.parse(value??"");return parsed&&typeof parsed==="object"&&!Array.isArray(parsed)?parsed as Record<string,number>:{};}catch{return {};}}

export default function QuizPlayer({quizId,title,questions,attempts:initialAttempts,maxAttempts,kidsMode}:{quizId:string;title:string;questions:Question[];attempts:Attempt[];maxAttempts:number|null;kidsMode:boolean}){
 const supabase=createClient();const startedAt=useRef(Date.now());const[answers,setAnswers]=useState<Record<string,Answer>>({});const[attempts,setAttempts]=useState(initialAttempts);const[retrying,setRetrying]=useState(initialAttempts.length===0);const[submitting,setSubmitting]=useState(false);const[error,setError]=useState<string|null>(null);const canRetry=maxAttempts===null||attempts.length<maxAttempts;const best=attempts.reduce<Attempt|null>((b,a)=>!b||a.score/a.total>b.score/b.total?a:b,null);
 async function submit(){setSubmitting(true);setError(null);let score=0;const total=questions.reduce((s,q)=>s+(q.points??1),0);for(const q of questions){const answer=answers[q.id];const pts=q.points??1;let ok=false;if(q.type==="complete")ok=String(answer??"").trim().toLocaleLowerCase()===String(q.correctAnswer??q.options[0]??"").trim().toLocaleLowerCase();else if(q.type==="numerical")ok=Number(answer)===Number(q.correctAnswer??q.options[0]);else if(q.type==="multiple_select") {const expected=parseNumbers(q.correctAnswer);const actual=Array.isArray(answer)?answer:[];ok=expected.length===actual.length&&expected.every(x=>actual.includes(x));}else if(q.type==="matching"){const expected=parseMap(q.correctAnswer);const actual=answer&&typeof answer==="object"&&!Array.isArray(answer)?answer as Record<string,number>:{};const keys=Object.keys(expected);ok=keys.length===Object.keys(actual).length&&keys.every(k=>Number(actual[k])===Number(expected[k]));}else if(q.type==="ordering"){const expected=parseNumbers(q.correctAnswer,q.options.map((_,i)=>i));const actual=Array.isArray(answer)?answer:[];ok=expected.length===actual.length&&expected.every((x,i)=>x===actual[i]);}else ok=answer===q.correctIndex;if(ok)score+=pts;}
 const{data:{user}}=await supabase.auth.getUser();if(!user){setError("انتهت الجلسة. سجّل الدخول مرة أخرى.");setSubmitting(false);return;}const{error:insertError}=await supabase.from("quiz_attempts").insert({quiz_id:quizId,user_id:user.id,score,total,answers,duration_seconds:Math.max(1,Math.round((Date.now()-startedAt.current)/1000))});if(insertError){setError("تعذر حفظ النتيجة. حاول مرة أخرى.");setSubmitting(false);return;}setAttempts(a=>[...a,{score,total}]);setAnswers({});setRetrying(false);setSubmitting(false);}
 if(!retrying)return <div className="border-t border-line pt-4 mt-2"><p className={kidsMode?"text-base font-bold text-[#7a3fa0] mb-2":"text-sm font-medium text-ink mb-1"}>{title}</p><span className="inline-block text-xs bg-green-light text-green-text px-2.5 py-1 rounded-full">أعلى نتيجة: {best?.score} من {best?.total}</span><p className="text-xs text-ink-faint mt-2">عدد المحاولات: {attempts.length}{maxAttempts!==null?` من ${maxAttempts}`:""}</p>{canRetry?<button onClick={()=>{startedAt.current=Date.now();setRetrying(true)}} className="mt-3 bg-green text-white text-sm font-medium py-2 px-5 rounded-md">إعادة المحاولة</button>:<p className="text-xs text-ink-faint mt-3">خلصت المحاولات المتاحة.</p>}</div>;
 const allAnswered=questions.every(q=>{const a=answers[q.id];if(a===undefined)return false;if(Array.isArray(a))return a.length>0;if(a&&typeof a==="object")return Object.keys(a).length===q.options.length/2;return String(a).trim()!=="";});
 return <div className="border-t border-line pt-4 mt-2 space-y-4"><p className={kidsMode?"text-base font-bold text-[#7a3fa0]":"text-sm font-medium text-ink"}>{kidsMode?"🎯 ":""}{title}</p>{questions.map((q,qi)=>{const current=answers[q.id];return <div key={q.id} className={kidsMode?"bg-[#fff8ec] border-2 border-[#ffd166] rounded-2xl p-4":"bg-paper rounded-lg p-4"}><div className="flex justify-between gap-3 mb-3"><p className="text-sm text-ink">{qi+1}. {q.question}</p><span className="text-[11px] text-ink-faint whitespace-nowrap">{q.points??1} نقطة</span></div>{q.reading_passage&&<div className="mb-4 rounded-xl bg-surface border border-line p-4 text-sm leading-7 text-ink whitespace-pre-wrap">{q.reading_passage}</div>}{q.media_url&&q.media_type==="image"&&<img src={q.media_url} alt="مرفق السؤال" className="max-h-64 w-full object-contain rounded-xl mb-4"/>}{q.media_url&&q.media_type==="audio"&&<audio controls src={q.media_url} className="w-full mb-4"/>}{q.type==="complete"?<input value={String(current??"")} onChange={e=>setAnswers(a=>({...a,[q.id]:e.target.value}))} placeholder="اكتب الإجابة هنا" className="w-full px-3 py-2 border border-line rounded-md bg-surface text-sm"/>:q.type==="numerical"?<input type="number" value={String(current??"")} onChange={e=>setAnswers(a=>({...a,[q.id]:e.target.value}))} placeholder="اكتب الرقم" className="w-full px-3 py-2 border border-line rounded-md bg-surface text-sm"/>:q.type==="matching"?<div className="space-y-2">{q.options.map((pair,oi)=>{const [left,...rest]=pair.split("=");const right=rest.join("=").trim();const map=current&&typeof current==="object"&&!Array.isArray(current)?current as Record<string,number>:{};return <label key={oi} className="grid grid-cols-1 sm:grid-cols-2 gap-2 items-center text-sm"><span className="text-ink">{left.trim()}</span><select value={map[String(oi)]??""} onChange={e=>setAnswers(a=>({...a,[q.id]:{...map,[oi]:Number(e.target.value)}}))} className="px-3 py-2 border border-line rounded-md bg-surface"><option value="">اختر المطابقة</option>{q.options.map((p,j)=>{const[, ...r]=p.split("=");return <option key={j} value={j}>{r.join("=").trim()}</option>})}</select></label>})}</div>:q.type==="ordering"?<div className="space-y-2">{q.options.map((opt,oi)=>{const order=Array.isArray(current)?current:[];return <div key={oi} className="flex items-center gap-2"><span className="w-7 h-7 rounded-full bg-surface border border-line flex items-center justify-center text-xs">{order.indexOf(oi)>=0?order.indexOf(oi)+1:"–"}</span><span className="flex-1 text-sm text-ink">{opt}</span><button type="button" onClick={()=>setAnswers(a=>{const cur=Array.isArray(a[q.id])?[...(a[q.id] as number[])]:[];const next=cur.includes(oi)?cur.filter(x=>x!==oi):[...cur,oi];return {...a,[q.id]:next};})} className="text-xs border border-line rounded-md px-2 py-1">{order.includes(oi)?"إلغاء":"اختيار"}</button></div>})}<p className="text-xs text-ink-faint">اختر العناصر بالترتيب الصحيح.</p></div>:<div className="space-y-1.5">{q.options.map((opt,oi)=>{const selected:number[]=Array.isArray(current)?current:[];const checked=q.type==="multiple_select"?selected.includes(oi):current===oi;return <label key={oi} className="flex items-center gap-2 text-sm text-ink-soft"><input type={q.type==="multiple_select"?"checkbox":"radio"} name={q.id} checked={checked} onChange={e=>{if(q.type==="multiple_select"){const cur=Array.isArray(answers[q.id])?[...(answers[q.id] as number[])]:[];setAnswers(a=>({...a,[q.id]:e.target.checked?[...cur,oi]:cur.filter(x=>x!==oi)}));}else setAnswers(a=>({...a,[q.id]:oi}));}}/>{opt}</label>})}</div>}</div>})}{error&&<p className="text-sm text-red-600">{error}</p>}<button onClick={submit} disabled={!allAnswered||submitting} className="bg-green text-white text-sm font-medium py-2 px-5 rounded-md disabled:opacity-50">{submitting?"جاري التصحيح…":"تسليم الإجابات"}</button></div>;
}
